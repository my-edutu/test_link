
import React from 'react';
import WebFallback from '../components/WebFallback';

const LiveStreamingScreen = () => {
    return <WebFallback title="Live Streaming" />;
};

export default LiveStreamingScreen;
